/*
 * UI Components Index
 * Barrel exports for easy importing of UI components
 */

export { Button } from './Button';
export { Typography } from './Typography';

// Re-export types for convenience
export type { ButtonProps, TypographyProps } from '../../types';